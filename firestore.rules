rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This is a default rule. All reads and writes are disabled.
    // You'll need to define specific rules for your collections.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
