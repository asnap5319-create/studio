rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to profile images.
    // Allow write access only for authenticated users to debug the upload issue.
    match /profile-images/{userId} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
