rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // WARNING: TEMPORARY DEBUGGING RULE
    // This allows all reads and writes to all files.
    // This is insecure and should be reverted after testing.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
