
'use client';

/**
 * SponsoredCard is deprecated in favor of global Adsterra Social Bar.
 * This file is kept as an empty component to prevent build breaks if referenced.
 */
export function SponsoredCard() {
  return null;
}
